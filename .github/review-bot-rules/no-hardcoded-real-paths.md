# 规则:测试/示例不得硬编码真实路径(HIGH)

开源仓库,测试与示例里的路径会公开 —— 不得泄露真实用户名 / 真实项目路径。

## 审查要点

- 测试 / 示例 / fixture 里出现 `/Users/<真实用户名>/...`、真实项目绝对路径、真实机器名等,标 HIGH。
- 一律用合成占位:`/Users/example/...`、`/tmp/vt-test-...`、`<cwd>` 等。
- 实测扫描得到的真实路径(如某次复现 bug 时的用户路径)不得直接写进提交。

## 严重级

提交含真实用户名 / 真实路径 → HIGH(隐私,合并前应改为合成占位)。
